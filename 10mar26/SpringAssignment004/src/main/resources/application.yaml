spring:
  application:
    name: SpringAssignment004
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
server:
  port: 2106
logging:
  level:
    root: INFO
